<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Trip Package Bookings</title>
    <style>
        /* Table styling remains the same */
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #ddd;
        }

        /* Adjusted styles for h1 and h3 */
        h1 {
            font-size: 2em; /* Example: Larger font size */
            margin-bottom: 10px; /* Example: Increased bottom margin */
            color: #333; /* Example: Darker text color */
        }
        h3 {
            font-size: 1.5em; /* Example: Slightly smaller font size */
            margin-top: 0; /* Example: Removed top margin */
            margin-bottom: 20px; /* Example: Increased bottom margin */
            color: #666; /* Example: Slightly lighter text color */
        }
    </style>
</head>
<body>
<div th:replace="@{Fragment/header}"></div>
    <h1>Trip Package Bookings</h1>
    
    <h3>Your Booking is Successful!</h3>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Trip Package</th>
                <th>Hotel</th>
                <th>Room Type</th>
                <th>Restaurant</th>
                <th>Participants</th>
                <th>Booking Date</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="booking : ${bookings}">
                <td th:text="${booking.id}"></td>
                <td th:text="${booking.tripPackage.name}"></td>
                <td th:text="${booking.hotel.title}"></td>
                <td th:text="${roomTypeMap[booking.id]}"></td> <!-- Access roomTypeMap using booking.id -->
                <td th:text="${booking.restaurant.title}"></td>
                <td th:text="${booking.participants}"></td>
                <td th:text="${booking.bookingDate}"></td>
            </tr>
        </tbody>
    </table>
    <div th:replace="@{Fragment/footer}"></div>
</body>
</html>
