<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Hotel Details</title>
    <!-- Favicon -->
    <link rel="icon" href="img/tourland2.png" type="image/png" />
       <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <script src="/bootstrap/js/bootstrap.bundle.js" type="text/javascript"></script>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />
    <!-- jQuery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <style>
        .star {
            color: gold;
        }
        .star.empty {
            color: lightgray;
        }
        .custom-bg {
            background-color: #00003f;
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-top: 20px;
        }
        .form-label {
            font-weight: bold;
        }
        .form-control {
            border-radius: 5px;
        }
        .form-control:focus {
            border-color: #00003f;
            box-shadow: 0 0 0 0.2rem rgba(0, 0, 63, 0.25);
        }
        .form-group {
            margin-bottom: 20px;
        }
        .btn-custom {
            background-color: #00003f;
            color: white;
            border-radius: 5px;
            border: none;
            padding: 10px 20px;
            font-weight: bold;
        }
        .btn-custom:hover {
            background-color: #001d4b;
            color: white;
        }
        .custom-card {
            background-color: #00003f;
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .member-image {
            max-width: 50px;
            border-radius: 50%;
            margin-right: 10px;
        }
       	.container1 {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
      
		.small-card-img {
		    object-fit: cover; /* Ensures the image covers the entire area */
		    height: 50px; /* Set a small height */
		    width: 50px; /* Set a small width */
		    margin-left: 15px; /* Adjust the space to the right of the image */
	        float: left;
		}
		.card-body {
            overflow: hidden; /* Ensure the content flows around the floated image */
        }

    </style>
</head>
<body>
	<div th:replace="@{Fragment/header}"></div>
    <div class="container mt-5 mx-5">
        <h2 th:text="${card.title}"></h2>
        <div class="d-flex align-items-center mt-3">
            <span th:each="star : ${#numbers.sequence(1, 5)}"
                  th:classappend="${star <= averageRating} ? 'star' : 'star empty'">&#9733;</span>
            <span th:text="${formattedAvgRating}"></span> / <span th:text="${reviewCount}"></span> reviews
        </div>
        <img th:src="@{|/uploads/cards/${card.hotel_id}/${card.image}|}" alt="Hotel Image" class="img-fluid mb-4"/>
        <p th:text="${card.description}"></p>

        <h1>Hotel Review</h1>
        <div th:if="${card != null}">
            <div class="card mb-4">
                <div class="card-body">
                    <h2 class="card-title" th:text="${card.title}">Hotel Name</h2>
                    <p class="card-text"><strong>Location:</strong> <span th:text="${card.location}">Location</span></p>
                    <p class="card-text"><strong>Description:</strong> <span th:text="${card.description}">Description</span></p>
                	 <div class="row">
           <div th:each="roomType : ${card.roomTypes}" class="col-md-2 mb-3">
                <div class="card">
                    <img th:src="@{/images/{imageFilename}(imageFilename=${roomType.imageFilename})}" 
                         class="small-card-img" alt="Room Image">
                    <div class="card-body">
                        <h5 class="card-title" th:text="${roomType.type}">Room Type</h5>
                        <p class="card-text">
                            <strong>Price:</strong> <span th:text="${roomType.price}">Price</span>
                        </p>
                    </div>
                </div>
            </div>
        </div>
                
                </div>
            </div>

            <div class="container1 mt-4">
                <h3>Reviews</h3>
                <ul class="list-group">
                    <li th:each="review : ${reviews}" class="list-group-item mb-3">
                        <div class="row">
                            <!-- Left column for member details -->
                            <div class="col member-details">
                                <div class="member-image">
                                    <img class="img-fluid rounded"  th:if="${review.member.imageUrl != null}"  th:src="@{|/uploads/members/${review.member.id}/${review.member.imageUrl}|}" alt="Member Image" />
                                     <img th:unless="${review.member.imageUrl != null}" src="/image/img/user.png" alt="Default Image" />
                                </div>
                                <h5 class="card-title mt-3"><strong>Member:</strong> <span th:text="${review.member.name}">No member</span></h5>
                            </div>
                            <!-- Right column for review details -->
                            <div class="col-md-8 review-details">
                                <div class="comment-box">
                                    <textarea class="form-control" rows="3" readonly th:text="${review.comment}">No comment</textarea>
                                </div>
                               <div class="d-flex align-items-center mt-2">
                                    <strong>Rating:</strong>
                                    <span th:each="star : ${#numbers.sequence(1, 5)}"
                                          th:classappend="${star <= review.rating} ? 'star' : 'star empty'" class="mx-1">&#9733;</span>
                                </div>
                            </div>
                        </div>
                    </li>
                </ul>
                <div class="mt-4">
                    <h3>Add Review</h3>
                    <form th:object="${reviewhotel}" th:action="@{/cards/{id}/reviews(id=${card.hotel_id})}" method="post">
                         <div class="mb-3">
                            <label for="rating" class="form-label">Rating:</label>
                           <div class="mb-3">
		                    <label for="rating" class="form-label">Rating:</label>
		                    <select id="rating" name="rating" class="form-control" required>
		                            <option value="5">5 Stars</option>
		                            <option value="4">4 Stars</option>
		                            <option value="3">3 Stars</option>
		                            <option value="2">2 Stars</option>
		                            <option value="1">1 Star</option>
		                        </select>
                			</div>
                        </div>
                        <div class="mb-3">
                            <label for="comment" class="form-label">Comment:</label>
                            <textarea class="form-control" id="comment" name="comment" rows="3" required></textarea>
                        </div>
                       
                        <button type="submit" class="btn btn-primary">Submit Review</button>
                    </form>
                </div>
            </div>
        </div>
        <div th:if="${card == null}">
            <p>Hotel not found.</p>
        </div>


    </div>
    <div th:replace="@{Fragment/footer}"></div>
</body>
</html>
