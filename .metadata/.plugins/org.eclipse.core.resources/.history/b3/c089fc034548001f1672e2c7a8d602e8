<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Trip Packages</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #ddd;
        }
    </style>
</head>
<body>
<div th:replace="@{Fragment/header}"></div>
    
    
    <h1>One Day Trips</h1>
    <table class="table table-hover table-striped table-bordered">
                            <thead class="table-primary">
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Description</th>
                                    <th>Image</th>
                                    <th>Location</th>
                                    <th>Price</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr th:each="package : ${packageList}">
                                    <td th:text="${package.package_id}"></td>
                                    <td th:text="${package.title}"></td>
                                    <td th:text="${package.description}"></td>
                                    <td>
                                        <img th:src="@{|/uploads/packages/{id}/{imageName}|(id=${package.package_id},imageName=${package.image})}" alt="Package Image" width="100">
                                    </td>
                                    <td th:text="${package.location}"></td>
                                    <td th:text="${package.price}"></td>
                                    <td>
                                        <a th:href="@{/package/edit/{id}(id=${package.package_id})}" class="btn btn-sm btn-primary">Edit</a>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                        
                        
                        
         <h1>One Week Trips</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Description</th>
                <th>Start Date</th>
                <th>End Date</th>
                <th>Price</th>
                <th>Image</th>
                <th>Hotels</th>
                <th>Restaurants</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="tripPackage : ${tripPackages}">
                <td th:text="${tripPackage.id}"></td>
                <td th:text="${tripPackage.name}"></td>
                <td th:text="${tripPackage.description}"></td>
                <td th:text="${tripPackage.startDate}"></td>
                <td th:text="${tripPackage.endDate}"></td>
                <td th:text="${tripPackage.price}"></td>
                <td> <img width="260px" height="200px" th:src="@{|/uploads/trippackages/${tripPackage.id}/${tripPackage.image}|}" alt="Image" /></td>
                <td>
                    <ul>
                        <li th:each="hotel : ${tripPackage.hotels}" th:text="${hotel.title}"></li>
                    </ul>
                </td>
                <td>
                    <ul>
                        <li th:each="restaurant : ${tripPackage.restaurants}" th:text="${restaurant.title}"></li>
                    </ul>
                </td>
            </tr>
        </tbody>
    </table>
    <div th:replace="@{Fragment/footer}"></div>
</body>
</html>
