<!DOCTYPE html>
<html xmlns:th="www.thymeleaf.org">
<head>
<meta charset="ISO-8859-1">
<title>View Members</title>
<link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
<script src="bootstrap/js/bootstrap.bundle.js" type="text/javascript"></script>


</head>
<body>

<div th:replace="fragment/header1"></div>
  
<div class="container">
<h1 >View Item Information</h1>
	<div class="alert alert-primary" role="alert" th:if="${success}" th:text="${success}"></div>

	<table class="table table-hover table-success">
	<tr>
		<th>ID</th>
		<th>Name</th>
		<th>UserName</th>
		<th>Password</th>
		<th>Email</th>
		<th>Role</th>
		<th>Action</th>
		
	</tr>
	<tr th:each="member:${memberList}">
	<td th:text="${member.id}"></td>
	<td th:text="${member.name}" ></td>
	<td th:text="${member.username}"></td>
	<td th:text="${member.password}"></td>
	<td th:text="${member.email}"></td>
	<td th:text="${member.role}"></td>
	<td><a th:href="@{/member/edit/{id} (id=${member.id})}" >Edit</a>
	<a th:href="@{/member/delete/{id} (id=${member.id})}" > | Delete</a>
	</td>
	</tr>
	
	</table>
	
	<form th:action="@{/member/add}" method="get">
	<input class="form-control" type="submit" value="Add Member">
	</form>
	
	<div th:replace="fragment/footer"></div>
</body>
</div>
</html>