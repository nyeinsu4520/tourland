<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <!-- Custom CSS for additional styling -->
    <style>
        body {
            background-color: #f8f9fa;
            color: #495057;
            font-family: Arial, sans-serif;
        }
        .container {
            max-width: 500px;
            margin-top: 100px;
            text-align: center;
        }
        .card {
            padding: 20px;
            border: none;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
        }
        .card-header {
            background-color: #28a745;
            color: white;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            padding: 15px;
        }
        .btn-custom {
            background-color: #00003f;
            color: white;
            border: none;
        }
        .btn-custom:hover {
            background-color: #000027;
        }
        .btn-outline-secondary {
            border-color: #00003f;
            color: #00003f;
        }
        .btn-outline-secondary:hover {
            background-color: #00003f;
            color: white;
        }
    </style>
</head>
<body>
    
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h4>Reset Password</h4>
            </div>
            <div class="card-body">
                <p class="text-info">You have to fill the phone number and email that the user entered when you created the account.</p>
                <form th:action="@{/reset-password}" method="post">
                    <div class="form-group">
                        <label for="username">Username:</label>
                        <input type="text" id="username" name="username" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="phonenum">Phone Number:</label>
                        <input type="text" id="phonenum" name="phonenum" class="form-control" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email:</label>
                        <input type="email" id="email" name="email" class="form-control" required>
                    </div>
                    <button type="submit" class="btn btn-custom">Reset Password</button>
                </form>
                <div th:if="${token != null}" class="mt-3">
                    <p>Reset token: <span th:text="${token}"></span></p>
                    <p>
                        <a th:href="@{${resetLink}}" class="btn btn-custom">Click here to change your password</a>
                    </p>
                </div>
                <div th:if="${error != null}" class="mt-3">
                    <p class="text-danger" th:text="${error}"></p>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
