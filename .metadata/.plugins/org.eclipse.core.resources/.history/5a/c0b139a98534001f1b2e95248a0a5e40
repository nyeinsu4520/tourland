<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Create Trip Package</title>
</head>
<body>
    <form th:action="@{/admin/trip-packages/create}" th:object="${tripPackage}" method="post">
        <!-- General Trip Package Details -->
        <label>Trip Package Name: <input type="text" th:field="*{name}"/></label><br/>
        <label>Description: <textarea th:field="*{description}"></textarea></label><br/>
        <label>Start Date: <input type="date" th:field="*{startDate}"/></label><br/>
        <label>End Date: <input type="date" th:field="*{endDate}"/></label><br/>
        
        <!-- Select Hotels -->
        <label>Select Hotel(s):
            <select th:field="*{cards}" multiple="multiple">
                <option th:each="hotel : ${allHotels}" th:value="${hotel.hotel_id}" th:text="${hotel.title}"></option>
            </select>
        </label><br/>
        
        <!-- Select Restaurants -->
        <label>Select Restaurant(s):
            <select th:field="*{restaurants}" multiple="multiple">
                <option th:each="restaurant : ${allRestaurants}" th:value="${restaurant.restaurant_id}" th:text="${restaurant.title}"></option>
            </select>
        </label><br/>
        
        <button type="submit">Create Trip Package</button>
    </form>
</body>
</html>
