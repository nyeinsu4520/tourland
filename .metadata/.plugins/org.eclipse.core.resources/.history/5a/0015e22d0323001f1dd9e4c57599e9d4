<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Hotel Details</title>
</head>
<body>
    <h1>Hotel Details</h1>
    <div th:if="${card != null}">
        <h2 th:text="${card.title}">Hotel Name</h2>
        <p><strong>Location:</strong> <span th:text="${card.location}">Location</span></p>
        <p><strong>Description:</strong> <span th:text="${card.description}">Description</span></p>

       <h3>Reviews</h3>
		<ul>
    		<li th:each="review : ${card.reviewshotel}">
        		<span th:text="${review.comment}">Review Comment</span> -
        		<span th:text="${review.rating}">Rating</span>
    		</li>
		</ul>

        <h3>Add Review</h3>
        <form th:object="${reviewhotel}" th:action="@{/cards/{id} (id=${card.hotel_id})}" method="get">
            <label for="comment">Comment:</label>
            <input type="text" id="comment" name="comment"><br>
            <label for="rating">Rating:</label>
            <input type="number" id="rating" name="rating" min="1" max="5"><br>
            <button type="submit">Submit</button>
        </form>
    </div>
    <div th:if="${card == null}">
        <p>Hotel not found.</p>
    </div>
    <a href="/index">Back to Hotels</a>
</body>
</html>
