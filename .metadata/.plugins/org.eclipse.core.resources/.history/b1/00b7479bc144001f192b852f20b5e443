<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trip Packages</title>
    
    <!-- Favicon -->
    <link rel="icon" href="img/tourland2.png" type="image/png" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">

    <!-- Font Awesome CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet" />

    <!-- Custom CSS -->
    <style>
        .star {
            color: gold;
        }
        .star.empty {
            color: lightgray;
        }
        .card {
            margin-bottom: 20px;
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .favorite-icon {
            position: absolute;
            top: 10px;
            right: 10px;
            z-index: 1;
        }
    </style>
</head>
<body>
    <div th:replace="@{Fragment/header}"></div>

    <div class="container mt-5">
        <h2 class="text-center mb-4">Trip Packages</h2>
        
        <div class="row">
            <!-- Iterate over tripPackages -->
            <div th:each="tripPackage, iterStat : ${tripPackages}" class="col-sm-6 col-md-4 mb-4">
                <div class="card h-100">
                    <div class="favorite-icon">
                        <i class="far fa-heart"></i>
                    </div>
                    <a th:href="@{/trippackage/view/{id} (id=${tripPackage.id})}">
                        <img th:src="@{|/uploads/trippackages/${tripPackage.id}/${tripPackage.image}|}" class="card-img-top" alt="Trip Package Image" />
                    </a>
                    <div class="card-body">
                        <h5 class="card-title" th:text="${tripPackage.name}"></h5>
                        <p class="card-text" th:text="${tripPackage.description}"></p>
                        <p class="card-text">From MMK <span th:text="${tripPackage.price}"></span> per person</p>
                        <!-- Star rating -->
                        <p>Rating: 
                            <span th:each="star : ${#numbers.sequence(1, 5)}"
                                  th:classappend="${star <= tripPackage.averageRating} ? 'star' : 'star empty'">&#9733;</span>
                        </p>
                    </div>
                </div>
            </div>
            <!-- End iteration -->
        </div>
    </div>
    
    <div th:replace="@{Fragment/footer}"></div>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
