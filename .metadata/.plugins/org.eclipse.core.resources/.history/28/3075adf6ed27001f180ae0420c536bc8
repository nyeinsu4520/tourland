<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Hotel Details</title>
</head>
<body>
    <h1>Hotel Details</h1>
    <div th:if="${card != null}">
        <h2 th:text="${card.title}">Hotel Name</h2>
        <p><strong>Location:</strong> <span th:text="${card.location}">Location</span></p>
        <p><strong>Description:</strong> <span th:text="${card.description}">Description</span></p>

       <h3>Reviews</h3>
		 <ul>
        <li th:each="review : ${reviews}">
        	<p><strong>Member:</strong> <span th:text="${review.member.name}">No member</span></p>
            <p><strong>Comment:</strong> <span th:text="${review.comment}">No comment</span></p>
            <p><strong>Rating:</strong> <span th:text="${review.rating}">No rating</span></p>
        </li>
    </ul>

        <h3>Add Review</h3>
   <form th:object="${reviewhotel}" th:action="@{/cards/reviews/{id}(id=${card.hotel_id})}" method="post">
    <label for="comment">Comment:</label>
    <input type="text" id="comment" name="comment"><br>
    <label for="rating">Rating:</label>
    <input type="number" id="rating" name="rating" min="1" max="5"><br>
    <!-- No need for memberId input field here -->
    <button type="submit">Submit</button>
</form>


    </div>
    <div th:if="${card == null}">
        <p>Hotel not found.</p>
    </div>
    <a href="/index">Back to Hotels</a>
</body>
</html>
