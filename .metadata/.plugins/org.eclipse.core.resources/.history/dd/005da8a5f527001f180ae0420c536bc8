<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Package Review</title>
</head>
<body>
    <h1>Package Review</h1>
    <div th:if="${package != null}">
        <h2 th:text="${package.title}">Package Name</h2>
        <p><strong>Location:</strong> <span th:text="${package.location}">Location</span></p>
        <p><strong>Description:</strong> <span th:text="${package.description}">Description</span></p>

       <h3>Reviews</h3>
		 <ul>
        <li th:each="reviewpackages: ${reviewpackage}">
        	<p><strong>Member:</strong> <span th:text="${reviewpackage.member.name}">No member</span></p>
            <p><strong>Comment:</strong> <span th:text="${reviewpackage.comment}">No comment</span></p>
            <p><strong>Rating:</strong> <span th:text="${reviewpackage.rating}">No rating</span></p>
        </li>
    </ul>

        <h3>Add Review</h3>
   <form th:object="${reviewPackage}" th:action="@{/package/{id}/reviews(id=${package.package_id})}" method="post">
    <label for="comment">Comment:</label>
    <input type="text" id="comment" name="comment"><br>
    <label for="rating">Rating:</label>
    <input type="number" id="rating" name="rating" min="1" max="5"><br>
    <!-- No need for memberId input field here -->
    <button type="submit">Submit</button>
</form>


    </div>
    <div th:if="${package == null}">
        <p>Package not found.</p>
    </div>
    <a href="/index">Back to Package</a>
</body>
</html>
