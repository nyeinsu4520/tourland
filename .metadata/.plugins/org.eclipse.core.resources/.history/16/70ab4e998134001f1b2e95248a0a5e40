<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Create Trip Package</title>
</head>
<body>
    <h2>Create a New Trip Package</h2>
    <form th:object="${tripPackage}" th:action="@{/trip-packages/create}" method="post">
        <label for="name">Name:</label><br>
        <input class="form-control" type="text" id="name" th:field="*{name}" name="name"><br>
        
        <label for="description">Description:</label><br>
        <textarea class="form-control" id="description" th:field="*{description}" name="description"></textarea><br>
        
        <label for="startDate">Start Date:</label><br>
        <input class="form-control" type="date" id="startDate" th:field="*{startDate}" name="startDate"><br>
        
        <label for="endDate">End Date:</label><br>
        <input class="form-control" type="date" id="endDate" th:field="*{endDate}" name="endDate"><br>
        
        <h3>Select Hotels</h3>
        <ul>
	    <li th:each="hotel : ${allHotels}">
	        <input type="checkbox" th:value="${hotel.hotel_id}" name="selectedHotels" th:id="${'hotel_' + hotel.hotel_id}">
	        <label th:for="${'hotel_' + hotel.hotel_id}" th:text="${hotel.title}"></label>
	        <!-- Optionally display hotel image or other details -->
	    </li>
		</ul>
		
		<h3>Select Restaurants</h3>
		<ul>
		    <li th:each="restaurant : ${allRestaurants}">
		        <input type="checkbox" th:value="${restaurant.restaurant_id}" name="selectedRestaurants" th:id="${'restaurant_' + restaurant.restaurant_id}">
		        <label th:for="${'restaurant_' + restaurant.restaurant_id}" th:text="${restaurant.title}"></label>
		        <!-- Optionally display restaurant image or other details -->
		    </li>
		</ul>
        <input type="submit" value="Create Trip Package">
    </form>
</body>
</html>
